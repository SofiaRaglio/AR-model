In Scripts you can find the scripts to run. Some of them are common for the two monkeys, some of them are separate. 
In kernels you can find the pre-run AR-model.
In data you can find the original data.
